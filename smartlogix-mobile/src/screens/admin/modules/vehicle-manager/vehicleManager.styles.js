import { StyleSheet } from 'react-native';
import { Colors, Fonts, Shadows } from '../../../../theme/ui';

export const styles = StyleSheet.create({
    container: { flex: 1, padding: 16 },
    headerRow: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center', marginBottom: 12 },
    title: { fontSize: 24, fontFamily: Fonts.bold, color: Colors.textPrimary },
    subtitle: { fontSize: 13, fontFamily: Fonts.medium, color: Colors.textSecondary, marginTop: 2 },
    addButton: { backgroundColor: Colors.brandOrange, borderRadius: 10, paddingHorizontal: 12, paddingVertical: 10 },
    addButtonText: { color: '#fff', fontFamily: Fonts.bold, fontSize: 13 },
    addActionButton: {
        backgroundColor: '#FFF8F0',
        borderWidth: 1,
        borderColor: '#F7C98F',
        borderRadius: 999,
        paddingHorizontal: 12,
        paddingVertical: 7,
        flexDirection: 'row',
        alignItems: 'center',
        gap: 5,
    },
    addActionButtonText: {
        color: Colors.brandOrange,
        fontSize: 12,
        fontFamily: Fonts.bold,
    },
    searchInput: {
        backgroundColor: Colors.surface,
        borderWidth: 1,
        borderColor: Colors.border,
        borderRadius: 10,
        paddingHorizontal: 12,
        paddingVertical: 10,
        marginBottom: 12,
        color: Colors.textPrimary,
        fontFamily: Fonts.regular,
    },
    card: {
        backgroundColor: Colors.surface,
        borderWidth: 1,
        borderColor: Colors.border,
        borderRadius: 14,
        padding: 14,
        marginBottom: 10,
        ...Shadows.card,
    },
    cardHeader: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center' },
    regNo: { fontSize: 16, fontFamily: Fonts.bold, color: Colors.textPrimary, flex: 1 },
    statusChip: { borderRadius: 999, paddingHorizontal: 10, paddingVertical: 4 },
    statusText: { color: '#fff', fontSize: 11, fontFamily: Fonts.semibold, textTransform: 'capitalize' },
    line: { marginTop: 6, fontSize: 13, color: Colors.textSecondary, fontFamily: Fonts.medium },
    actions: { flexDirection: 'row', gap: 8, marginTop: 12 },
    btn: { flex: 1, borderRadius: 10, paddingVertical: 10, alignItems: 'center' },
    btnPrimary: { backgroundColor: Colors.brandOrange },
    btnDanger: { backgroundColor: '#FEE2E2' },
    btnTextPrimary: { color: '#fff', fontFamily: Fonts.bold, fontSize: 13 },
    btnTextDanger: { color: Colors.danger, fontFamily: Fonts.bold, fontSize: 13 },
    empty: { alignItems: 'center', paddingVertical: 40 },
    emptyText: { marginTop: 8, fontSize: 14, color: Colors.textSecondary, fontFamily: Fonts.medium },
    formOverlay: { flex: 1, backgroundColor: 'rgba(0,0,0,0.45)', justifyContent: 'center', padding: 16 },
    formCard: { backgroundColor: Colors.surface, borderRadius: 16, padding: 16, maxHeight: '80%' },
    formScrollContent: { flexGrow: 1, paddingBottom: 20 },
    formLabel: {
        fontSize: 12,
        fontFamily: Fonts.semibold,
        color: '#6B7280',
        marginBottom: 6,
        marginTop: 4,
        textTransform: 'uppercase',
        letterSpacing: 0.4,
    },
    formTitle: { fontSize: 18, fontFamily: Fonts.bold, color: Colors.textPrimary, marginBottom: 12 },
    formField: {
        backgroundColor: '#F9FAFB',
        borderWidth: 1,
        borderColor: Colors.border,
        borderRadius: 10,
        paddingHorizontal: 12,
        paddingVertical: 10,
        marginBottom: 10,
        fontFamily: Fonts.regular,
        color: Colors.textPrimary,
    },
    inputIconWrap: {
        flexDirection: 'row',
        alignItems: 'center',
        backgroundColor: '#F9FAFB',
        borderWidth: 1,
        borderColor: Colors.border,
        borderRadius: 12,
        paddingHorizontal: 12,
        minHeight: 52,
        marginBottom: 10,
    },
    formFieldWithIcon: {
        flex: 1,
        marginLeft: 10,
        fontFamily: Fonts.regular,
        color: Colors.textPrimary,
        fontSize: 15,
        minHeight: 50,
    },
    filterRow: { flexDirection: 'row', gap: 8, marginBottom: 10 },
    filterChip: { borderWidth: 1, borderColor: Colors.border, borderRadius: 999, paddingHorizontal: 10, paddingVertical: 6, backgroundColor: Colors.surface },
    filterChipActive: { borderColor: Colors.brandOrange, backgroundColor: '#FFF8F0' },
    filterText: { fontSize: 12, fontFamily: Fonts.medium, color: Colors.textSecondary },
    filterTextActive: { color: Colors.brandOrange, fontFamily: Fonts.bold },
    selectField: {
        minHeight: 56,
        borderRadius: 16,
        borderWidth: 1,
        borderColor: '#D1D5DB',
        backgroundColor: '#F9FAFB',
        paddingHorizontal: 16,
        flexDirection: 'row',
        alignItems: 'center',
        justifyContent: 'space-between',
        marginBottom: 10,
    },
    selectFieldText: {
        fontSize: 18,
        lineHeight: 24,
        color: '#111827',
        fontFamily: Fonts.medium,
        flex: 1,
        marginRight: 10,
    },
    sheetOverlay: {
        flex: 1,
        backgroundColor: 'rgba(0,0,0,0.35)',
        justifyContent: 'center',
        padding: 16,
    },
    sheetCard: {
        backgroundColor: Colors.surface,
        borderRadius: 18,
        padding: 14,
        maxHeight: '70%',
    },
    pickerCard: {
        backgroundColor: Colors.surface,
        borderRadius: 18,
        padding: 14,
    },
    sheetTitle: {
        fontSize: 16,
        fontFamily: Fonts.bold,
        color: Colors.textPrimary,
        marginBottom: 8,
    },
    sheetOptions: {
        maxHeight: 280,
    },
    sheetOption: {
        paddingVertical: 12,
        borderBottomWidth: 1,
        borderBottomColor: '#F3F4F6',
    },
    sheetOptionText: {
        fontSize: 15,
        fontFamily: Fonts.medium,
        color: Colors.textPrimary,
    },
    formActions: { flexDirection: 'row', justifyContent: 'flex-end', gap: 8, marginTop: 8 },
    formBtn: { borderRadius: 10, paddingHorizontal: 14, paddingVertical: 10 },
    formCancel: { backgroundColor: '#F3F4F6' },
    formSubmit: { backgroundColor: Colors.brandOrange },
    formCancelText: { fontSize: 13, fontFamily: Fonts.bold, color: Colors.textSecondary },
    formSubmitText: { fontSize: 13, fontFamily: Fonts.bold, color: '#fff' },
});
